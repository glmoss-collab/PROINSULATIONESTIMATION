# Lawrenceville / MPA Gwinnett MOB — takeoff test

Uploaded set: mechanical drawings (11 sheets) + specs (178 pages, Section 230700).

Run:

```bash
python3 estimates/gwinnett_mob/calculate_gwinnett_takeoff.py
```

Outputs: `takeoff.json`, `GWINNETT_MOB_BID.txt`.

Rectangular SF: `2 × (W + H + 2t) / 12 × LF`. Do not use the engine’s “first number as diameter” path.
